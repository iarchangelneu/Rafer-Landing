server {
       listen 80;
       listen [::]:80;

       server_name rafer.kz wwww.rafer.kz;

       root /var/www/rafer;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}